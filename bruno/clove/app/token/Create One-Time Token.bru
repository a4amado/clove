meta {
  name: Create One-Time Token
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/v1/apps/{{app_id}}/tokens/
  body: json
  auth: inherit
}

body:json {
  {
    "channel_id": "ssssssssssss"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
