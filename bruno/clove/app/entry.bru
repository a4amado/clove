meta {
  name: entry
  type: http
  seq: 3
}

post {
  url: http://localhost:8080/api/v1/apps/{{app_id}}/entry/
  body: json
  auth: inherit
}

body:json {
  {
    "payload": "fasdasdasds"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
